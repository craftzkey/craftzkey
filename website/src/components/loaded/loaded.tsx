import React from 'react';
import ReactDOM from 'react-dom/client';
import Website from '../craftz/index';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Website />
  </React.StrictMode>
)
